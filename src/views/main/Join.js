import { lazy } from "react";
import { Link } from "react-router-dom";

const Join = () => {
    return (
            <h2>
                join 페이지
            </h2>
    );
  };
  
  export default Join;